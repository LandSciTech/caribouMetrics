% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fillDefaults.R
\name{fillDefaults}
\alias{fillDefaults}
\title{Generate default scenario parameters}
\usage{
fillDefaults(
  scns = NULL,
  iF = 0,
  iA = 0,
  aS = 0,
  aSf = 4,
  rS = 1,
  sS = 1,
  rQ = 0.5,
  sQ = 0.5,
  J = 20,
  P = 10,
  N0 = 1000,
  adjustR = FALSE,
  assessmentYrs = 1,
  ri = NA,
  cmult = NA,
  cw = NA,
  st = NA,
  iYr = NA,
  curYear = 2023
)
}
\arguments{
\item{scns}{a data.frame with column names matching the arguments below. Any
columns that are missing will be filled with the default values.}

\item{iF}{number. Initial fire disturbance percentage.}

\item{iA}{number. Initial anthropogenic disturbance percentage}

\item{aS}{number. Percent change in anthropogenic disturbance per year in the
observation period}

\item{aSf}{number. Percent change in anthropogenic disturbance per year in
the projection period}

\item{rS}{number. Disturbance-recruitment slope multiplier}

\item{sS}{number. Disturbance-survival slope multiplier}

\item{rQ}{number in 0, 1. Recruitment quantile}

\item{sQ}{number in 0, 1. Survival quantile}

\item{J}{Number of years of projections}

\item{P}{Number of years of observations}

\item{N0}{number. Initial population size}

\item{adjustR}{Logical. Adjust R to account for delayed age at first reproduction (DeCesare et al. 2012; Eacker et al. 2019).}

\item{assessmentYrs}{number. #TODO:}

\item{ri}{number. Optional. Number of years between collar deployments. If
missing assumed to be every year}

\item{cmult}{number. Optional. If provided number of cows is \code{cmult} \*
number of surviving collared cows at month 5}

\item{cw}{Optional. Only used in \code{\link[=runScnSet]{runScnSet()}} to set the number of cows per
year in recruitment survey}

\item{st}{Optional. If provided target number of deployed collars is overwritten with
this value.}

\item{iYr}{year. First year in observation period. Optional, if not provided
it will be calculated from \code{curYear} and \code{P}}

\item{curYear}{year. The current year. All years before are part of the
observation period and years after are part of the projection period.}
}
\value{
a data.frame of parameter values including a label that combines all
the parameter names and values into a string
}
\description{
This function can be used to generate default parameter values used in
\code{\link[=simulateObservations]{simulateObservations()}}
}
\examples{
fillDefaults()

# scns list takes precedence over argument values
fillDefaults(scns = data.frame(iF = 10, iA = 20, P = 1), P = 5)

}
\references{
DeCesare, N.J., Hebblewhite, M., Bradley, M., Smith, K.G.,
Hervieux, D. and Neufeld, L., 2012. Estimating ungulate recruitment and
growth rates using age ratios. The Journal of Wildlife Management, 76(1),
pp.144-153.

Eacker, D.R., Hebblewhite, M., Steenweg, R., Russell, M., Flasko, A. and
Hervieux, D., 2019. Web‐based application for threatened woodland caribou
population modeling. Wildlife Society Bulletin, 43(1), pp.167-177.
}
